{
  "name": "temporal-replay-guard",
  "description": "Blocks `git push` in TypeScript Temporal projects when the commits being pushed would break in-flight workflow executions, and invokes a skill that fetches the latest Temporal versioning and testing docs to guide the fix.",
  "version": "0.1.0",
  "author": {
    "name": "Anthony Wong"
  }
}
